{
  "name": "@tokenpact/agent-provider",
  "version": "0.1.0",
  "description": "Example provider agent: produces an output artifact for a task.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node --import ../../tools/ts-run.mjs src/index.ts",
    "dev": "node --import ../../tools/ts-run.mjs src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(add eslint here)\" && exit 0",
    "test": "node --test"
  },
  "dependencies": {
    "@tokenpact/core": "workspace:*"
  }
}
