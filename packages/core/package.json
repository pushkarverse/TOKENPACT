{
  "name": "@tokenpact/core",
  "version": "0.1.0",
  "description": "Shared domain model for TokenPact: task specs, verification results, and the escrow state machine.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(add eslint here)\" && exit 0",
    "test": "node --test"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
